import { LoginForm } from "./components/login-form";

const LoginPage = () => {
  return (
    <div className="min-h-screen bg-ivory py-20 flex items-center justify-center px-4">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
